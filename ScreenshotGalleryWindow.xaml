<Window x:Class="WindowsSimpleCapture.ScreenshotGalleryWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="截图画廊" Height="700" Width="1000"
        WindowStartupLocation="CenterScreen"
        Background="#F8F9FA">
    
    <Window.Resources>
        <!-- 分类按钮样式 -->
        <Style x:Key="CategoryButtonStyle" TargetType="Button">
            <Setter Property="Background" Value="#E9ECEF"/>
            <Setter Property="Foreground" Value="#495057"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Padding" Value="12,6"/>
            <Setter Property="FontSize" Value="12"/>
            <Setter Property="Margin" Value="5,0"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}"
                                CornerRadius="15"
                                Padding="{TemplateBinding Padding}">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Background" Value="#007ACC"/>
                                <Setter Property="Foreground" Value="White"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
        
        <!-- 操作按钮样式 -->
        <Style x:Key="ActionButtonStyle" TargetType="Button">
            <Setter Property="Background" Value="#007ACC"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Padding" Value="10,5"/>
            <Setter Property="FontSize" Value="11"/>
            <Setter Property="Margin" Value="2"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}"
                                CornerRadius="3"
                                Padding="{TemplateBinding Padding}">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Background" Value="#0056B3"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
        
        <!-- 缩略图样式 -->
        <Style x:Key="ThumbnailStyle" TargetType="Border">
            <Setter Property="Background" Value="White"/>
            <Setter Property="BorderBrush" Value="#DEE2E6"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="CornerRadius" Value="8"/>
            <Setter Property="Margin" Value="10"/>
            <Setter Property="Padding" Value="8"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Effect">
                <Setter.Value>
                    <DropShadowEffect Color="#000000" Opacity="0.1" 
                                    BlurRadius="5" ShadowDepth="2"/>
                </Setter.Value>
            </Setter>
        </Style>
    </Window.Resources>
    
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        
        <!-- 标题栏 -->
        <Border Grid.Row="0" Background="#007ACC" Padding="20,15">
            <StackPanel Orientation="Horizontal">
                <TextBlock Text="🖼️ 截图画廊" 
                          FontSize="18" FontWeight="Bold" 
                          Foreground="White" VerticalAlignment="Center"/>
                <TextBlock x:Name="TotalCountText" 
                          Text="(共 0 张截图)" 
                          FontSize="12" 
                          Foreground="#B3D9FF" 
                          VerticalAlignment="Center" 
                          Margin="10,0,0,0"/>
            </StackPanel>
        </Border>
        
        <!-- 分类和操作栏 -->
        <Border Grid.Row="1" Background="White" Padding="20,15" 
                BorderBrush="#E9ECEF" BorderThickness="0,0,0,1">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>
                
                <!-- 分类按钮 -->
                <StackPanel Grid.Column="0" Orientation="Horizontal">
                    <TextBlock Text="分类:" FontSize="12" Foreground="#6C757D" 
                              VerticalAlignment="Center" Margin="0,0,10,0"/>
                    <Button x:Name="DateCategoryBtn" Content="📅 按日期" 
                           Style="{StaticResource CategoryButtonStyle}" 
                           Click="DateCategory_Click"/>
                    <Button x:Name="ComputerCategoryBtn" Content="💻 按计算机" 
                           Style="{StaticResource CategoryButtonStyle}" 
                           Click="ComputerCategory_Click"/>
                    <Button x:Name="WindowCategoryBtn" Content="🪟 按窗口" 
                           Style="{StaticResource CategoryButtonStyle}" 
                           Click="WindowCategory_Click"/>
                </StackPanel>
                
                <!-- 操作按钮 -->
                <StackPanel Grid.Column="1" Orientation="Horizontal">
                    <Button x:Name="RefreshBtn" Content="🔄 刷新" 
                           Style="{StaticResource ActionButtonStyle}" 
                           Click="Refresh_Click"/>
                    <Button x:Name="CopySelectedBtn" Content="📋 复制选中" 
                           Style="{StaticResource ActionButtonStyle}" 
                           Click="CopySelected_Click" IsEnabled="False"/>
                    <Button x:Name="DeleteSelectedBtn" Content="🗑️ 删除选中" 
                           Style="{StaticResource ActionButtonStyle}" 
                           Click="DeleteSelected_Click" IsEnabled="False"/>
                </StackPanel>
            </Grid>
        </Border>
        
        <!-- 主内容区域 -->
        <ScrollViewer Grid.Row="2" VerticalScrollBarVisibility="Auto" 
                     HorizontalScrollBarVisibility="Disabled">
            <StackPanel x:Name="ContentPanel" Margin="20">
                <!-- 动态生成的分类内容将在这里显示 -->
            </StackPanel>
        </ScrollViewer>
        
        <!-- 状态栏 -->
        <Border Grid.Row="3" Background="#F8F9FA" Padding="20,10" 
                BorderBrush="#E9ECEF" BorderThickness="0,1,0,0">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>
                
                <TextBlock x:Name="StatusText" 
                          Text="准备就绪" 
                          FontSize="11" 
                          Foreground="#6C757D" 
                          VerticalAlignment="Center"/>
                
                <TextBlock x:Name="SelectedCountText" 
                          Grid.Column="1"
                          Text="已选择: 0 张" 
                          FontSize="11" 
                          Foreground="#007ACC" 
                          VerticalAlignment="Center"/>
            </Grid>
        </Border>
    </Grid>
</Window>